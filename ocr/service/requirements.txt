# Python OCR Microservice Requirements
# =====================================
# Python >= 3.9 required.  Tested on 3.12.
#
# PaddlePaddle CPU build  (for GPU: pip install paddlepaddle-gpu)
paddlepaddle==2.6.2
paddleocr==2.9.1

# Web framework
flask==3.1.0
flask-cors==5.0.0

# Image processing (headless avoids Qt dependency on servers)
opencv-python-headless==4.12.0.88
Pillow==11.0.0
numpy==2.1.2

# Misc
python-dotenv==1.0.1
requests==2.32.3
